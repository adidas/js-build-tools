{
  "name": "javascript-babel-node",
  "private": true,
  "scripts": {
    "build": "babel src -d dist",
    "test": "jest --config jest.config.js",
    "clean": "rimraf dist && rimraf coverage",
    "all": "npm run clean && npm run build && npm run test"
  },
  "devDependencies": {
    "babel-jest": "~23.6.0",
    "build-tools-babel": "~1.0.0",
    "build-tools-babel-jest": "~1.0.0",
    "build-tools-common": "~1.0.0",
    "build-tools-javascript": "~1.0.0",
    "build-tools-jest": "~1.0.0"
  }
}
